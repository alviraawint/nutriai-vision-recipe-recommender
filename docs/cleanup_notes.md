# Cleanup Notes

This repository was reorganized for a public GitHub portfolio.

## Kept In The Main Project

- Runtime app files at the root
- Database files under `database/`
- Selected demo model under `models/`
- Training/evaluation metadata under `results/training_outputs/`

## Archived

Older helper scripts, duplicate model experiments, local IDE files, Python cache files, and the local virtual environment were moved to `archive_unused/` instead of being deleted.
